---
- name: install apache in ec2 with tag OS centos
  hosts: tag_OS_centos
  user: ec2-user
  sudo: yes
  tasks:
  - name: Install latest apache
    yum:
      name: "{{ item }}"
      state: latest
    with_items:
      - httpd
      - php